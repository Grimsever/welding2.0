@model IEnumerable<WeldingV2._0.Models.Foremen>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SecondName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SecondName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ForemenId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ForemenId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ForemenId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
